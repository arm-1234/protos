syntax = "proto3";

package clinic.v1.types;

import "google/protobuf/timestamp.proto";
import "clinic/v1/types/enums/appointment_status.proto";
import "clinic/v1/types/enums/consultation_mode.proto";

option go_package = "github.com/arm-1234/protos/clinic/v1/types;types";

// A bookable time window returned by ListAvailableSlots.
message SlotInfo {
  google.protobuf.Timestamp starts_at = 1;
  google.protobuf.Timestamp ends_at = 2;
  // False when the slot is already taken or blocked by staff.
  bool is_available = 3;
  // Remaining capacity; a branch may run parallel chairs for a service.
  int32 remaining_capacity = 4;
}

// Patient-supplied details captured at booking time. The clinic identifies
// patients by phone number rather than requiring an account.
message PatientDetails {
  string full_name = 1;
  string phone = 2;
  string email = 3;
  int32 age = 4;
  string gender = 5;
  // Free-text referral source from the intake slip ("Referred By").
  string referred_by = 6;
  // Symptoms or reason for visit supplied by the patient.
  string notes = 7;
}

message AppointmentInfo {
  string appointment_id = 1;
  // Short human-friendly code shared with the patient, e.g. "DV-8F3K2Q".
  string reference_code = 2;
  string service_id = 3;
  string service_name = 4;
  string doctor_id = 5;
  string doctor_name = 6;
  string branch_id = 7;
  string branch_name = 8;
  PatientDetails patient = 9;
  google.protobuf.Timestamp scheduled_at = 10;
  int32 duration_minutes = 11;
  clinic.v1.types.enums.ConsultationMode mode = 12;
  clinic.v1.types.enums.AppointmentStatus status = 13;
  // Internal staff-only notes; never returned on public endpoints.
  string staff_notes = 14;
  string cancellation_reason = 15;
  google.protobuf.Timestamp created_at = 16;
  google.protobuf.Timestamp updated_at = 17;
}
